{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hola creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('hola') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
